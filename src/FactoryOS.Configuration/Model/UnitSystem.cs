namespace FactoryOS.Configuration.Model;

/// <summary>The unit system a tenant reports measurements in.</summary>
public enum UnitSystem
{
    /// <summary>Metric units (SI).</summary>
    Metric = 0,

    /// <summary>Imperial units.</summary>
    Imperial = 1,
}
